Command: deleteRaid

@param (args[0] = raidName)	STRING

Deletes a raid, given it exists in availableRaids.
Also deletes the raid sign up message, if one such exists.

Example:
'~deleteRaid trainingRaid'