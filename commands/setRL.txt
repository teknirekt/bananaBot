Command: setRL(raidName, discordName(optional))

1. raidName = name of the raid for which you're trying to set the raid leader
2. discordName = if you're trying to set another person as the raid leader.

Sets author of message/discordName as raid leader of the raid.

Example:
'~setRL trainingRaid'